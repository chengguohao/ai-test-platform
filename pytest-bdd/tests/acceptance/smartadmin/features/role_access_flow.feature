# language: zh-CN
@smartadmin @acceptance
功能: SmartAdmin RBAC 跨角色企业流程（权限序列）

  作为测试经理，我希望用 Gherkin 描述「管理员新增 → 填报员填报 → 审核员审核 →
  越权打回 → 填报员重提 → 管理员归档」的跨角色链路，
  以便角色协同与越权语义可在评审稿/AI 生成阶段直接复用。

  背景:
    假如 SmartAdmin 会话已经完成登录（由 api_client fixture session 级自动登录）

  场景大纲: 管理员-填报员-审核员 跨角色企业流程
    当 以 "admin" 身份调用 企业 创建接口，企业名="<企业名>"
    那么 业务信封 返回成功
    当 以 "reporter" 身份调用 企业 修改接口，企业名="<企业名>"
    那么 业务信封 返回成功
    当 以 "auditor" 身份调用 企业 详情接口，企业名="<企业名>"
    那么 业务信封 返回成功
    当 以 "auditor" 身份调用 企业 修改接口，企业名="<企业名>"
    那么 业务信封 返回失败（越权无权限）
    当 以 "reporter" 身份调用 企业 修改接口，企业名="<企业名>"
    那么 业务信封 返回成功
    当 以 "admin" 身份调用 企业 删除接口，企业名="<企业名>"
    那么 业务信封 返回成功

    例子:
      | 企业名                     |
      | RBAC_BDD_A_${ts_token}     |
      | RBAC_BDD_B_${ts_token}     |